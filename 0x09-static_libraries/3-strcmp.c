#include <string.h>

/**
 * _strcmp - the function that compare the value of two variables
 *
 * @s1: pointer one array
 *
 * @s2: pointer two array
 *
 * Return: return an interger.
 */
int _strcmp(char *s1, char *s2)
{
	int a = strcmp(s1, s2);

	return (a);
}
