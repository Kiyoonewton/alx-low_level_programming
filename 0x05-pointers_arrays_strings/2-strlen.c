#include <stdio.h>
#include <string.h>

/**
 * _strlen - determine the length of a string
 *
 * @s: char pointer
 *
 * Return: length
 */

int _strlen(char *s)
{
	int a;

	a = strlen(s);
	return (a);
}
