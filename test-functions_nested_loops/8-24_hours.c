#include "main.h"

/**
 * jack_bauer - print out the minutes in 24hrs
 *
 * Return: void
*/

void jack_bauer(void)
{
	int  b;

	// for (a = 0; a < 2; a++)
	// {
		for (b = 0; b < 10; b++)
		{
			// _putchar('0' + a);
			// _putchar('0' + a);
			// _putchar(':');
			_putchar(b + '0');
			// _putchar(b % 10);
			_putchar('\n');
		}
	// }
}
